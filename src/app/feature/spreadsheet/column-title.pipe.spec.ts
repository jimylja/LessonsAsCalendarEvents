import { ColumnTitlePipe } from './column-title.pipe';

describe('ColumnTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
